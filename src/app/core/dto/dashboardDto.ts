export interface DashboardDto {
    /**
     * Id.
     * 
     * The dashboard id which comes from the backend.
     */
    id: number;

    /**
     * Header.
     * 
     * The dashboard header.
     */
    header: string;
}